import '../styles/globals.css'
//this is the only place you can import the globals.css stylesheet
function MyApp({ Component, pageProps }) {
  return <Component {...pageProps} />
}

export default MyApp
